export const onConfirmCreate = async (values) => {
  /*const newData = {
    ...values,
    account_info: { data: { type: 'A' } },
  };

  insertAsset({
    variables: {
      object: newData,
    },
    refetchQueries: 'active',
  });*/
};
