mutation updateUser($userId: uuid!, $name: String, $email: String) {
  update_users(where: { id: { _eq: $userId } }, _set: { name: $name, email: $email }) {
    returning {
      id
      name
    }
  }
}
