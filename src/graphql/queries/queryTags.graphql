query allTags($limit: Int, $offset: Int, $order_by: [tags_order_by!], $where: tags_bool_exp) {
  tags(limit: $limit, offset: $offset, order_by: $order_by, where: $where) {
    id
    name
    created_at
    updated_at
  }
  tags_aggregate(where: $where) {
    aggregate {
      count
    }
  }
}
